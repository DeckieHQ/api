machine:
  pre:
    - sudo pip install -U docker-compose==1.5.2
  services:
    - docker

dependencies:
  override:
    - docker-compose --version

database:
  override:
    - echo "Skipping database preparation..."

test:
  override:
    - make test
